---
description: altfins-query-builder project guidance
alwaysApply: true
---

This project includes the local altFINS skill `{{SKILL_NAME}}` at `{{PROJECT_SKILL_ROOT}}`.

When a user starts from a vague crypto research or trading request:
- read `{{PROJECT_SKILL_ROOT}}/README.md` to confirm this skill is the right first step
- read `{{PROJECT_SKILL_ROOT}}/SKILL.md` before translating the request
- use the bundled references and assets inside `{{PROJECT_SKILL_ROOT}}/`
- route the request only toward validated CLI or MCP target families
- keep the handoff conservative when the user has not provided enough detail
